## Configuration du nœud 2
node.name=node2
node.port=8081
node.host=localhost
node.internal.token=secret_token_123
node.isLeader=false

## Configuration du cluster
cluster.nodes=http://localhost:8080,http://localhost:8081,http://localhost:8082

## Configuration de la base de données
database.name=memorydb2
database.path=./data/node2

## Paramètres de communication
network.timeout=30000
network.heartbeat.interval=5000

## Paramètres de performance
memory.limit=1024M
thread.pool.size=4 